package abstractconcept;

public interface InteF 
{
	//An interface alwayes define abstract methods
	//No method body
	//only method decleration 
	//we achive 100% abstraction
	//can not create the object of interfaces
	//In interface can not user abstract keyword because it's default abstraction
	//It declar only final and static-->but it's default in nature
	//When we use interface:when ever full of abstraction ilke only abstract methods use(business logic)
	//100% of ligin is hide then use interface
	//Performsnce wise is slow 
	//In Interface we can not create the consructor
	//No Static methods
	//No Main Method
	
	public void start();
	public void strop();
	public void reful();
	

	
}
